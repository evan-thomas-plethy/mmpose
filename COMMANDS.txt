IMPORTANT: use cuda-12.1 (expect to be default)
AND for every session:
conda activate mmpose
export PYTHONPATH=/home/ubuntu/visionAI/mmpose:$PYTHONPATH
    To resolve this issue: Traceback (most recent call last): 596, in call_endpointameter 'metrics[3].name' supplied: Names may only contain alphanumerics, underscores (_), dashes (-), periods (.), spaces ( ), colon(:) and slashes (/).

tmux new -s train
conda activate mmpose
Ctrl + B, D to detach
tmux attach -t train

To shutdown GPU safely
sudo shutdown -h +1
# If need to cancel this
sudo shutdown -c
##################################################################################

Please note: In the past, often renamed the current dataset as data/coco.
Do NOT do this anymore.

Prepare dataset:
(Preferably split dataset on dedup param 0.99 version, if applicable, to slightly reduce training time with smalled val set)
data/split_dataset.py
data/mirror_val_data.py

To reuse a previous val.json, use this to ensure videos remain atomically train/val 
data/filter_val_data.py
data/mirror_val_data.py

Train: 
Single experiment (default looks for dataset under data/coco/ with train2017/ , val2017/ , annotations/person_keypoints_train2017.json , annotations/person_keypoints_val2017.json):
python tools/train.py configs/body_2d_keypoint/rtmpose/coco/rtmpose-m_8xb256-420e_coco-256x192_finetune.py \
    --mlflow-experiment-name "rtmpose-m_ground_based_exercises_chromakey_and_dedup" \
    --mlflow-run-name "chromakey_v3"

Sweep (v1 dataset sizes, frozen backbone):
python experiments/run_sweep.py experiments/sweep_config_backbone_unfreeze_v4.yaml
# v2 flat unfreeze ladder (seed2_n1000): sweep_config_backbone_unfreeze_v2.yaml
# v3 ld0.5 unfreeze ladder (seed2_n1000): sweep_config_backbone_unfreeze_v3.yaml
# v4 warmup ablations: sweep_config_backbone_unfreeze_v4.yaml
sudo shutdown -h +1
# Options:
#   --dry-run              Print commands without executing
#   --experiment NAME      Run only a specific experiment

Test: 
# Test on any validation set (preferred): explicit annotation JSON + images folder
# (file_name entries in the JSON are loaded relative to --val-images-dir)
python tools/test.py configs/body_2d_keypoint/rtmpose/coco/rtmpose-m_8xb256-420e_coco-256x192_finetune.py \
    mmpose/models/_models/pose/rtmpose-m_simcc-aic-coco_pt-aic-coco_420e-256x192-63eb25f7_20230126.pth \
    --val-annotations data/coco_ground_based_exercises_v5/annotations/person_keypoints_val2017.json \   
    --val-images-dir data/coco_ground_based_exercises_v5/val2017 \
    --work-dir work_dirs/eval_ground_based_exercises_v5_pretrained \
    --dump ground_based_exercises_v5_pretrained.pkl

# Same eval, but append heel_slides/* COCO metrics only to an existing MLflow training run
# (match --mlflow-experiment-name and --mlflow-run-name to that run; does not log new params)
python tools/test.py configs/body_2d_keypoint/rtmpose/coco/rtmpose-m_8xb256-420e_coco-256x192_finetune.py \
    work_dirs/rtmpose-m_ground_based_exercises_lr_sweep/lr_5e-5/best_coco_AP_epoch_43.pth \
    --val-annotations data/heel_slides_v2_train_val/annotations/person_keypoints_val2017.json \
    --val-images-dir data/heel_slides_v2_train_val/val2017 \
    --metric-prefix heel_slides \
    --mlflow-experiment-name "rtmpose-m_ground_based_exercises_lr_sweep" \
    --mlflow-run-name "lr_5e-5" \
    --mlflow-append-to-existing-run \
    --work-dir work_dirs/eval_ground_based_exercises \
    --dump ground_based_testing_heel_slides.pkl

# Alternative: COCO-style data root + ann path relative to root + image subfolder
# python tools/test.py ... \
#     --val-data-root data/Heel_Slides/v1/ \
#     --val-ann-file annotations/person_keypoints_val2017.json \
#     --val-img-prefix frames/

# Copy run metrics/artifacts from one mlflow experiment to another
# Update this file accordingly, then run
python tools/copy_mlflow_run_metrics.py

Vis:
python tools/visualize_results.py --results ground_based_exercises_v5_pretrained.pkl --images-dir data/ground_based_exercises_v5/val2017 --output ground_based_exercises_v5_pretrained.mp4
OR if file not found due to path being doubled
python tools/visualize_results.py --results ground_based_exercises_v5_pretrained.pkl --images-dir . --output ground_based_exercises_v5_pretrained.mp4

Export Results:
python tools/convert_pkl_to_json.py --results pretrain_det_standing_general_val.pkl --output pretrain_det_standing_general_val.json

Publish:
python tools/misc/publish_model.py mmpose/models/_models/pose/rtmpose-m_simcc-aic-coco_pt-aic-coco_420e-256x192-63eb25f7_20230126.pth ./rtmpose-m_publish.pth

Export Model: -- Unsure if this is deprecated. Be sure to review evan_thomas mmdeploy repo for most up to date.
Using mmpose env: run the code in test.py to torchscript export
Using model_conversion_env: torch_to_coreml.py